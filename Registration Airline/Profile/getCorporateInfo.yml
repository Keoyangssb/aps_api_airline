info:
  name: getCorporateInfo
  type: http
  seq: 1

http:
  method: POST
  url: https://apiairline-aps.aidctech.net/api/RegisterAirline/GetReviewRegistration
  body:
    type: json
    data: |-
      {
        "register_id": "199"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
