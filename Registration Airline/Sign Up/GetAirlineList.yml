info:
  name: GetAirlineList
  type: http
  seq: 5

http:
  method: GET
  url: "{{currentIP}}/api/RegisterAirline/GetAirlineList"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
