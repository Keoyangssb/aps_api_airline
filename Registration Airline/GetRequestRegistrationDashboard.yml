info:
  name: GetRequestRegistrationDashboard
  type: http
  seq: 4

http:
  method: GET
  url: "{{currentIP}}/api/RegisterAirline/GetRequestRegistrationDashboard"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
