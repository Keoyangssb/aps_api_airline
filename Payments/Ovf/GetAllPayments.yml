info:
  name: GetAllPayments
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/Payment/GetAllPayments"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
