info:
  name: GetInvoiceInfoDetail
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/PaymentLanding/GetInvoiceInfoDetail"
  body:
    type: json
    data: "{\r\n  \"id\": \"14030\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
