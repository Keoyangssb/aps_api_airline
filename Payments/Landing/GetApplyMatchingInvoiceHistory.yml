info:
  name: GetApplyMatchingInvoiceHistory
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/PaymentLanding/GetApplyMatchingInvoiceHistory"
  body:
    type: json
    data: "{\r

      \  \"customer_code\": \"CRK\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
