info:
  name: GetInvoiceTracking_by_invoice_id
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/Receipt/GetInvoiceTracking_by_invoice_id"
  body:
    type: json
    data: "{\r\n  \"id\": \"3799\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
