info:
  name: generate_e_receipt
  type: http
  seq: 1

http:
  method: POST
  url: "{{currentIP}}/api/ReceiptLanding/generate_e_receipt"
  body:
    type: json
    data: "{\r

      \  \"transaction_id\": \"1080\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
