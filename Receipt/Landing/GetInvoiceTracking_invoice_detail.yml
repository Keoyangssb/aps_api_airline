info:
  name: GetInvoiceTracking_invoice_detail
  type: http
  seq: 1

http:
  method: POST
  url: "{{currentIP}}/api/ReceiptLanding/GetInvoiceTracking_invoice_detail"
  body:
    type: json
    data: "{\r\n  \"id\": \"3799\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
