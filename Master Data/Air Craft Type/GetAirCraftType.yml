info:
  name: GetAirCraftType
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetAirCraftType"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
