info:
  name: DeleteAirCraftType
  type: http
  seq: 5

http:
  method: POST
  url: "{{currentIP}}/api/MasterData/DeleteAirCraftType"
  body:
    type: json
    data: "{\r\n  \"id\" : \"590\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
