info:
  name: DeleteAirCraft
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/MasterData/DeleteAirCraft"
  body:
    type: json
    data: "{\r\n  \"id\": \"1\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
