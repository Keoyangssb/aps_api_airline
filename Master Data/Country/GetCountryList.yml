info:
  name: GetCountryList
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetCountryList"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
