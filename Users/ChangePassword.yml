info:
  name: ChangePassword
  type: http
  seq: 16

http:
  method: POST
  url: https://apiairline-aps.aidctech.net/api/User/ChangePassword
  body:
    type: json
    data: |-
      {
        "new_password": "123",
        "otp": "365745"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
