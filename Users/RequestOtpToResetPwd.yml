info:
  name: RequestOtpToResetPwd
  type: http
  seq: 15

http:
  method: POST
  url: "{{currentIP}}/api/User/RequestOtpToResetPwd"
  body:
    type: json
    data: "{\r

      \  \"userEmail\": \"littavanh.p.aidctech@gmail.com\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
