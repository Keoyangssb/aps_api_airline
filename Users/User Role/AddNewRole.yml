info:
  name: AddNewRole
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/UserRole/AddNewRole"
  body:
    type: json
    data: "{\r

      \  \"role_name\": \"test rrrrr\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
