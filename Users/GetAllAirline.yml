info:
  name: GetAllAirline
  type: http
  seq: 9

http:
  method: GET
  url: "{{currentIP}}/api/User/GetAirlineList"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
