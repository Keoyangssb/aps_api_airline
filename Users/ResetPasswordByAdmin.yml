info:
  name: ResetPasswordByAdmin
  type: http
  seq: 17

http:
  method: POST
  url: "{{currentIP}}/api/User/ResetPasswordByAdmin"
  body:
    type: json
    data: "{\r\n  \"userId\": \"5\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
