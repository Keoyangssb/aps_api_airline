info:
  name: RequestNewOtp
  type: http
  seq: 13

http:
  method: POST
  url: "{{currentIP}}/api/User/RequestNewOtp"
  body:
    type: json
    data: "{\r

      \  \"user_email\": \"littavanh.p.aidctech@gmail.com\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
