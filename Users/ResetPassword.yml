info:
  name: ResetPassword
  type: http
  seq: 16

http:
  method: POST
  url: "{{currentIP}}/api/User/ResetPassword"
  body:
    type: json
    data: "{\r

      \  \"newPassword\": \"123\",\r

      \  \"opt\": \"365745\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
