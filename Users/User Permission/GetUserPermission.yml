info:
  name: GetUserPermission
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/UserRole/GetUserPermission"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
