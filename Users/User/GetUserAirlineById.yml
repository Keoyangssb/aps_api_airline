info:
  name: GetUserAirlineById
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/User/GetUserAirlineById"
  body:
    type: json
    data: "{\r\n    \"user_id\": \"15\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
