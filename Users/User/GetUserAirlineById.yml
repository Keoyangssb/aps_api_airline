info:
  name: GetUserAirlineById
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/User/GetUserAirlineById"
  body:
    type: json
    data: |-
      {
          "user_id": "147"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
