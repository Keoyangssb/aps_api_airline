info:
  name: GetPriorityList
  type: http
  seq: 2

http:
  method: GET
  url: "{{currentIP}}/api/Ticket/GetPriorityList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
