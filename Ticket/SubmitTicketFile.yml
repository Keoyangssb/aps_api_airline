info:
  name: SubmitTicketFile
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/Ticket/SubmitTicketFile?id=3"
  params:
    - name: id
      value: "3"
      type: query
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /C:/Users/ASUS/Pictures/aidc/ovf_invoice.jpg

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
