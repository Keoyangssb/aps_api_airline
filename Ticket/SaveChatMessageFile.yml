info:
  name: SaveChatMessageFile
  type: http
  seq: 8

http:
  method: POST
  url: "{{currentIP}}/api/Ticket/SaveChatMessageFile?id=1"
  params:
    - name: id
      value: "1"
      type: query
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /C:/Users/ASUS/Pictures/aidc/MOF-APS _ Ministry of Finance - Air Aviation Payment System.pdf
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
