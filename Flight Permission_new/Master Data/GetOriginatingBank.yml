info:
  name: GetOriginatingBank
  type: http
  seq: 12

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetOriginatingBank"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
