info:
  name: GetReceivingBank
  type: http
  seq: 13

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetReceivingBank"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
