info:
  name: GetAirlinesByAgent
  type: http
  seq: 14

http:
  method: GET
  url: https://apiairline-aps.aidctech.net/api/FlightPermission/GetAirlinesByAgent
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
