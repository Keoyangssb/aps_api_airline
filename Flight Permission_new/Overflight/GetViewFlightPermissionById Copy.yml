info:
  name: GetViewFlightPermissionById Copy
  type: http
  seq: 10

http:
  method: POST
  url: 10.0.100.61/airline/api/FlightPermission/GetViewFlightPermissionById
  body:
    type: json
    data: "{\r\n  \"flight_per_id\": \"28\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
