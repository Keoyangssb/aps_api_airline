info:
  name: GetRequestNo_ovf
  type: http
  seq: 2

http:
  method: GET
  url: "{{currentIP}}/api/FlightPermission/GetRequestNo_ovf"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
