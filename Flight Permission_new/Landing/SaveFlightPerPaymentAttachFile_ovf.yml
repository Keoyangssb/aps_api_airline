info:
  name: SaveFlightPerPaymentAttachFile_ovf
  type: http
  seq: 12

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermission/SaveFlightPerPaymentAttachFile_ovf?flight_per_id=1"
  params:
    - name: flight_per_id
      value: "1"
      type: query
  body:
    type: multipart-form
    data:
      - name: payment_transfer_slip_file
        type: file
        value:
          - /C:/Users/ASUS/Documents/upload/CyberSecurity.pdf
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
