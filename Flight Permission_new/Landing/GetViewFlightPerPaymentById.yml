info:
  name: GetViewFlightPerPaymentById
  type: http
  seq: 12

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermissionLanding/GetViewFlightPerPaymentById"
  body:
    type: json
    data: "{\r\n  \"flight_per_id\": \"47\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
