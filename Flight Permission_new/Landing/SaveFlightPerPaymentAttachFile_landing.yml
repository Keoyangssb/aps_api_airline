info:
  name: SaveFlightPerPaymentAttachFile_landing
  type: http
  seq: 11

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermissionLanding/SaveFlightPerPaymentAttachFile_landing?flight_per_id=1"
  params:
    - name: flight_per_id
      value: "1"
      type: query
  body:
    type: multipart-form
    data:
      - name: payment_transfer_slip_file
        type: file
        value:
          - /C:/Users/ASUS/Documents/upload/CyberSecurity.pdf
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
