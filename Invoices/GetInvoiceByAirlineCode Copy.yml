info:
  name: GetInvoiceByAirlineCode Copy
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/Invoice/GetInvoiceByAirlineCode"
  body:
    type: json
    data: "{\r\n  \"airlineCode\": \"BMW\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
