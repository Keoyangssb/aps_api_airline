info:
  name: get_fr24_data
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5044/api/FR24/get_fr24_data?airlineCode=code&pageNumber=1&pageSize=10
  params:
    - name: airlineCode
      value: code
      type: query
    - name: pageNumber
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
