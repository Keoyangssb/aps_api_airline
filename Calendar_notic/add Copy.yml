info:
  name: add Copy
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5044/api/calendar/add
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
