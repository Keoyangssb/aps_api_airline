info:
  name: GetAccountBalance_ovf
  type: http
  seq: 1

http:
  method: POST
  url: "{{currentIP}}/api/AccountBalance/GetAccountBalance_ovf"
  body:
    type: json
    data: |-
      {
        "year": "2026",
        "customer_code": "CRK",
        "user_id": "147",
        "operator_type_id": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
