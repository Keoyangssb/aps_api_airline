info:
  name: GetInvoiceAmountAndFlightsComparison_local
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:6002/api/DashboardLanding/GetInvoiceAmountAndFlightsComparison
  body:
    type: json
    data: |-
      {
        "year": "2026",
        "customer_code": "AGL",
        "user_id": "200",
        "operator_type_id": "2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
