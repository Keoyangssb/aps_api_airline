info:
  name: GetYear
  type: http
  seq: 5

http:
  method: GET
  url: "{{currentIP}}/api/Dashboard2/GetYear"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
