info:
  name: GetNumberOfFlightsCompare
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/Dashboard2/GetNumberOfFlightsCompare"
  body:
    type: json
    data: "{\r

      \  \"year\": \"2025\",\r

      \  \"customer_code\": \"SIA\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
