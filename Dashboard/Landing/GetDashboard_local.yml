info:
  name: GetDashboard_local
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:6002/api/DashboardLanding/GetDashboard
  body:
    type: json
    data: |-
      {
        "year": "2026",
        "customer_code": "SIA",
        "user_id": "163",
        "operator_type_id": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
