info:
  name: GetDashboard
  type: http
  seq: 1

http:
  method: POST
  url: "{{currentIP}}/api/Dashboard2/GetDashboard"
  body:
    type: json
    data: "{\r

      \  \"year\": \"2024\",\r

      \  \"customer_code\": \"CRK\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
